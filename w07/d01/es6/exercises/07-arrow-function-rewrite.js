// Write a function `timesFour` that returns 4 times the value of a
// given number `n`

// YOUR_CODE_HERE
console.log( timesFour(15) );

// Rewrite the function as an arrow function

// const timesFourV2 = YOUR_CODE_HERE
console.log( timesFourV2(15) );

// Rewrite the function again as an arrow function with
// an implied return (no curly braces, no `return` statement)

// const timesFourV3 = YOUR_CODE_HERE
console.log( timesFourV3(15) );


// Bonus: do the same exercise with a function `isEven` which returns
//   true if the given number `n` is even and false otherwise

// Bonus 2: do the same exercise with a function `sayName` which, given
//   a String `name` and boolean `isAngry`, returns the `name` in uppercase
//   letters if `isAngry` is true and otherwise returns `name` as-is
